<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportsequipment.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="shippingAddress" column="shipping_address"/>
        <result property="phone" column="phone"/>
        <result property="paymentMethod" column="payment_method"/>
        <result property="recipientName" column="recipient_name"/>
        <result property="remark" column="remark"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <collection property="orderItems" column="id" select="findOrderItemsByOrderId"/>
    </resultMap>

    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="OrderResultMap">
        SELECT * FROM `order`
    </select>

    <insert id="insert" parameterType="Order">
        INSERT INTO `order` (user_id, total_amount, status, shipping_address, created_at, updated_at)
        VALUES (#{userId}, #{totalAmount}, #{status}, #{shippingAddress}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="Order">
        UPDATE `order` SET
        user_id = #{userId},
        total_amount = #{totalAmount},
        status = #{status},
        shipping_address = #{shippingAddress},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM `order` WHERE id = #{id}
    </delete>

    <select id="findByUserId" resultMap="OrderResultMap">
        SELECT * FROM `order` WHERE user_id = #{userId}
    </select>

    <resultMap id="OrderItemResultMap" type="OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="price" column="price"/>
    </resultMap>

    <select id="findOrderItemsByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

</mapper>
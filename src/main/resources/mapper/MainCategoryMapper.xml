<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportsequipment.mapper.MainCategoryMapper">

    <select id="findById" resultType="MainCategory">
        SELECT * FROM main_category WHERE id = #{id}
    </select>

    <select id="findAll" resultType="MainCategory">
        SELECT * FROM main_category
    </select>

    <insert id="insert" parameterType="MainCategory">
        INSERT INTO main_category (name, description, image_url, created_at, updated_at)
        VALUES (#{name}, #{description}, #{imageUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="MainCategory">
        UPDATE main_category SET
        name = #{name},
        description = #{description},
        image_url = #{imageUrl},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM main_category WHERE id = #{id}
    </delete>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportsequipment.mapper.UserMapper">

    <select id="findById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultType="User">
        SELECT * FROM user
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO user (username, email, password, phone, address, avatar, role, created_at, updated_at)
        VALUES (#{username}, #{email}, #{password}, #{phone}, #{address}, #{avatar}, #{role}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user SET
        username = #{username},
        email = #{email},
        password = #{password},
        phone = #{phone},
        address = #{address},
        avatar = #{avatar},
        role = #{role},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findByUsername" resultType="User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="existsByUsername" resultType="Integer">
        SELECT COUNT(*) FROM user WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="Integer">
        SELECT COUNT(*) FROM user WHERE email = #{email}
    </select>

</mapper>
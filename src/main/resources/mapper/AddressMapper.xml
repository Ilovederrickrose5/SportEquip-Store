<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportsequipment.mapper.AddressMapper">

    <select id="findById" resultType="Address">
        SELECT * FROM address WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Address">
        SELECT * FROM address
    </select>

    <insert id="insert" parameterType="Address">
        INSERT INTO address (user_id, recipient_name, phone, province, city, district, detail_address, is_default, created_at, updated_at)
        VALUES (#{userId}, #{recipientName}, #{phone}, #{province}, #{city}, #{district}, #{detailAddress}, #{isDefault}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="Address">
        UPDATE address SET
        user_id = #{userId},
        recipient_name = #{recipientName},
        phone = #{phone},
        province = #{province},
        city = #{city},
        district = #{district},
        detail_address = #{detailAddress},
        is_default = #{isDefault},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM address WHERE id = #{id}
    </delete>

    <select id="findByUserId" resultType="Address">
        SELECT * FROM address WHERE user_id = #{userId}
    </select>

    <select id="findByUserIdAndIsDefaultTrue" resultType="Address">
        SELECT * FROM address WHERE user_id = #{userId} AND is_default = true
    </select>

    <delete id="deleteByUserId" parameterType="Long">
        DELETE FROM address WHERE user_id = #{userId}
    </delete>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportsequipment.mapper.ReviewMapper">

    <select id="findById" resultType="Review">
        SELECT * FROM review WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Review">
        SELECT * FROM review
    </select>

    <insert id="insert" parameterType="Review">
        INSERT INTO review (product_id, user_id, rating, comment, created_at)
        VALUES (#{productId}, #{userId}, #{rating}, #{comment}, #{createdAt})
    </insert>

    <update id="update" parameterType="Review">
        UPDATE review SET
        product_id = #{productId},
        user_id = #{userId},
        rating = #{rating},
        comment = #{comment}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM review WHERE id = #{id}
    </delete>

    <select id="findByProductId" resultType="Review">
        SELECT * FROM review WHERE product_id = #{productId}
    </select>

    <select id="findByUserId" resultType="Review">
        SELECT * FROM review WHERE user_id = #{userId}
    </select>

    <select id="existsByUserIdAndProductId" resultType="Integer">
        SELECT COUNT(*) FROM review WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

</mapper>